# Secrets — never commit
.wh-credentials
.env

# Dependencies & build output
/vendor/
/node_modules/
composer.lock

# PHPUnit
.phpunit.result.cache
.phpunit.cache/

# Fallow analysis cache (the committed baselines live in .github/fallow-baselines/)
/.fallow/

# Playwright MCP scratch output (snapshots/screenshots from live inspection)
/.playwright-mcp/
